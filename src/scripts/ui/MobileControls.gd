extends CanvasLayer

# Mobile touch controls overlay.
# TouchScreenButton child nodes automatically fire Input actions,
# so keyboard controls continue to work alongside touch input.
# No additional scripting is required — layout and action bindings
# are configured entirely in MobileControls.tscn.
